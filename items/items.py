import cocos
import pyglet

class Item(cocos.tiles.Cell): # ? Come back to this, as not sure currently what module to instantiate. This will depend on how Item classes will work.

    def __init__(self):
        super().__init__()